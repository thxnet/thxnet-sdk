name: ARC probe
on:
  push:
    branches: ['tmp/**']
    paths: ['.github/workflows/arc-probe.yml']
  workflow_dispatch:

jobs:
  probe-light:
    runs-on: hetzner-thxnet-light
    steps:
      - run: |
          echo "runs-on=hetzner-thxnet-light (scale-set-name form)"
          hostname
          id
          uname -a
